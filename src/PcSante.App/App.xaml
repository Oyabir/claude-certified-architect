<Application x:Class="PcSante.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:infra="clr-namespace:PcSante.App.Infrastructure"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemesDictionary Theme="Light" />
                <ui:ControlsDictionary />
            </ResourceDictionary.MergedDictionaries>

            <infra:HealthBrushConverter x:Key="HealthBrush" />
            <infra:BoolToVisibilityConverter x:Key="Visible" />
            <infra:BoolToVisibilityConverter x:Key="Collapsed" Invert="True" />

            <!-- Accessibilité : texte d'au moins 14 px partout (section 11). -->
            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="PageTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="28" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="0,0,0,4" />
            </Style>

            <Style x:Key="PageSubtitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="15" />
                <Setter Property="Opacity" Value="0.8" />
                <Setter Property="Margin" Value="0,0,0,16" />
                <Setter Property="TextWrapping" Value="Wrap" />
            </Style>

            <Style x:Key="SectionTitle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="19" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="0,16,0,8" />
            </Style>

            <!-- Terme technique affiché en petit sous le libellé simple. -->
            <Style x:Key="Technical" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Opacity" Value="0.7" />
            </Style>

            <!-- Un seul bouton principal par écran : grand et coloré. -->
            <Style x:Key="PrimaryAction" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                <Setter Property="Appearance" Value="Primary" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="MinHeight" Value="52" />
                <Setter Property="MinWidth" Value="240" />
                <Setter Property="Padding" Value="24,10" />
            </Style>

            <Style x:Key="SecondaryAction" TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                <Setter Property="Appearance" Value="Secondary" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="MinHeight" Value="36" />
                <Setter Property="Padding" Value="14,6" />
                <Setter Property="Margin" Value="0,0,8,0" />
            </Style>

            <!-- Pastille de couleur (texte blanc sur fond coloré : contraste suffisant en thème clair et sombre). -->
            <Style x:Key="Badge" TargetType="Border">
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="Padding" Value="10,4" />
                <Setter Property="HorizontalAlignment" Value="Left" />
                <Setter Property="VerticalAlignment" Value="Center" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
